{
  "metadata": {
    "label": "Tobacco - T7 - Pharmacological cessation aids",
    "description": "Tobacco cessation pharmacotherapy intervention that provides cost-covered effective pharmacological interventions to all tobacco users who want to quit.\nThis scenario exposes the parameters for cessation medication policies that work by providing nicotine replacement therapy, prescription medications, and other pharmacological aids to support quit attempts.\nThe intervention reduces tobacco prevalence in the population by increasing successful quit rates through medication-assisted treatment, which in turn reduces the incidence rates of COPD among susceptible individuals.\nPharmacological interventions include nicotine replacement therapy (patches, gum, lozenges), prescription medications (bupropion, varenicline), and other evidence-based cessation aids.\nThese medications are most effective when combined with behavioral support and when made accessible through healthcare coverage and reduced cost barriers.\nNote: For a comprehensive understanding of tobacco control impacts, this intervention should also be run for Asthma, Diabetes, and Cardiovascular Disease models.\n",
    "authors": [],
    "date_created": "2025-08-11 17:17"
  },
  "parameters": {
    "Country": {
      "hidden": true,
      "value": "AFG",
      "paths": [
        "$.nodes[?(@.id=='lrso81aw')].generate_array.parameters.country",
        "$.nodes[?(@.id=='lrso71jf')].generate_array.parameters.country",
        "$.nodes[?(@.id=='lrso7mga')].generate_array.parameters.country",
        "$.nodes[?(@.id=='DsFreeSus')].generate_array.parameters.country",
        "$.nodes[?(@.id=='Mn5YBbsp')].generate_array.parameters.country",
        "$.nodes[?(@.id=='HvEhBiqV')].generate_array.parameters.country",
        "$.nodes[?(@.id=='wdoGuxrz')].generate_array.parameters.country",
        "$.links[?(@.id=='3gSm1z84')].generate_array.parameters.country",
        "$.links[?(@.id=='6dXmRQ7e')].generate_array.parameters.country",
        "$.links[?(@.id=='JPGqVn1u')].generate_array.parameters.country",
        "$.links[?(@.id=='k3JrWa6h')].generate_array.parameters.country",
        "$.links[?(@.id=='MfWskKJT')].generate_array.parameters.country",
        "$.links[?(@.id=='eBRm4kvr')].generate_array.parameters.country",
        "$.links[?(@.id=='WALgwDoi')].generate_array.parameters.country",
        "$.links[?(@.id=='HmI3Gkua')].generate_array.parameters.country",
        "$.links[?(@.id=='cz2LeDKw')].generate_array.parameters.country"
      ]
    },
    "Baseline Coverage Tobacco Quitcessation": {
      "value": 0.05,
      "description": "The baseline coverage for the intervention",
      "paths": [
        "$.nodes[?(@.id=='exp5DafX')].generate_array.parameters.value",
        "$.nodes[?(@.id=='ACrnBpRW')].generate_array.parameters.start_value",
        "$.nodes[?(@.id=='ACrnBpRW')].generate_array.parameters.fill_before"
      ],
      "category": "Coverage"
    },
    "Target Coverage Tobacco Quitcessation": {
      "value": 0.05,
      "description": "The target coverage for the intervention",
      "paths": [
        "$.nodes[?(@.id=='ACrnBpRW')].generate_array.parameters.end_value",
        "$.nodes[?(@.id=='ACrnBpRW')].generate_array.parameters.fill_after"
      ],
      "category": "Coverage"
    },
    "Scale-Up Start Year Tobacco Quitcessation": {
      "value": 1,
      "description": "The index when scale-up starts for this intervention, where 0 == the first year of the time horizon",
      "paths": [
        "$.nodes[?(@.id=='ACrnBpRW')].generate_array.parameters.start_index"
      ],
      "category": "Coverage"
    },
    "Scale-Up Final Year Tobacco Quitcessation": {
      "value": 2,
      "description": "The index when scale-up ends for this intervention, where 0 == the first year of the time horizon",
      "paths": [
        "$.nodes[?(@.id=='ACrnBpRW')].generate_array.parameters.end_index"
      ],
      "category": "Coverage"
    }
  }
}